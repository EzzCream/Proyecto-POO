
server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl.auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/laPape?zeroDateTimeBehavior=convertToNull&serverTimezone=UTC
    username: root
    password: 1234